def laske_summa(luvut):
    summa = sum(luvut)
    return summa

def laske_summa(luvut):
    summa = sum(luvut)
    return summa

luvut = [3, 7, 12, 5, 9]
summa = laske_summa(luvut)
print(f"Listan lukujen summa on {summa}.")